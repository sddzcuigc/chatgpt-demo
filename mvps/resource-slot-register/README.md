# Resource Slot Register

用于登记空闲 GitHub 仓库、Vercel Project 或其他实验资源的临时占用，避免“可以复用”演变成覆盖活跃项目。

## 核心路径

确认空闲证据 → 设定租期和负责人 → 明确隔离目录/分支 → 写退出条件与保护边界 → 输出是否可复用。

## 运行

直接打开 `index.html`。数据仅保存在浏览器 LocalStorage。

## 验收

- 活跃资源必须阻断。
- 只有空闲证据、租期、负责人、隔离、退出和保护边界全部具备时，才显示“可安全复用”。
- 390px 下单栏，无横向溢出。
