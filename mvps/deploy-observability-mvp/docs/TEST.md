# 测试说明

1. 空表单：应显示“仍是黑盒故障”。
2. 载入 403 示例：应达到“可复用故障指纹”。
3. 删除回滚点：状态降级。
4. 删除上下文：状态降级。
5. 保存后刷新：历史记录保留。
6. 导出 Markdown：字段完整。
7. 390px：单栏，无明显横向溢出。

## 本地结果
- `npm run check`：通过。
- `npm run build`：通过。
- Node.js：`>=24 <25`。
- 真实浏览器截图：当前执行环境未完成，必须标记截图环境阻塞。
