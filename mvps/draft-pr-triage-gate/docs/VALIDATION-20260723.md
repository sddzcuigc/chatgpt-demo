# 真实 WIP 门禁验证（2026-07-23）

## 验证对象

仓库：`sddzcuigc/chatgpt-demo`

本次检查时开放 Draft PR：

- #4 `Deploy Observability`：保留；有明确核心路径与本地构建证据，仍缺 Preview 和真实截图。
- #5 `Artifact Access Gate`：保留；聚焦构建制品 403/410，属于 #4 的具体前置门禁。
- #6 `Draft PR Triage Gate`：保留；已实际把开放草稿从 6 个压缩到 3 个。

已关闭：

- #1：被后续实现完整继承。
- #2：缺少真实验证，停止占用活跃 WIP。
- #3：运行时版本检查已被 #4 与 #5 的部署诊断链承接。

## 验收结果

- 活跃 Draft PR 数量：3，满足上限 `≤3`。
- 每个保留项都有明确下一步，不存在默认无限保留。
- 没有新增第 4 个 Draft PR。
- 本轮只补充验证证据，没有创建相似 MVP。

## 下一步门禁

在 #4、#5、#6 中至少一个发生以下事件前，不新增 Draft PR：

1. 获得真实 Preview；
2. 完成一次非作者验证；
3. 转为 Ready for review；
4. 合并或关闭。

## 当前阻塞

- `chatgpt-demo` 对应 Vercel Project 仍使用 Node.js 18.x，不能作为本轮安全 Preview 承载位。
- 当前自动化环境中的 Chromium 截图进程持续超时，未生成真实桌面与移动截图。

## 决策规则

当 WIP 已达到上限时，下一步只能是验证、合并、关闭或明确暂停；不能继续新增。